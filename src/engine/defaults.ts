// Central defaults: sizes in pixels, line height in em, corner radius as a fraction.
const DEFAULTS = Object.freeze({
  font_size: 16,
  font_family: 'IBM Plex Sans',
  font_weight: 400,
  color: 'black',
  line_height: 1.2,
  tab_size: 4,
  shape_height: 16,
  rounded_radius: 0.125,
  stroke_width: 1,
  stroke_miterlimit: 4,
  fill: 'none',
  stroke: 'black',
})

export { DEFAULTS }
