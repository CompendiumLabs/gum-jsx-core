// Pixel defaults, except line height, which is a multiple of the local font size.
const DEFAULTS = Object.freeze({
  font_size: 16,
  line_height: 1.2,
  shape_height: 16,
  stroke_width: 1,
  fill: 'none',
  stroke: 'black',
});

export { DEFAULTS };
